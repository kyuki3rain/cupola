# Implementation Plan

- [ ] 1. GitWorktree trait に fetch メソッドを追加し、アダプターで実装する
- [ ] 1.1 (P) port trait にリモート取得メソッドを定義する
  - リモートリポジトリから最新情報を取得する操作を trait メソッドとして追加する
  - 戻り値は既存メソッドと同様に `Result<()>` とする
  - _Requirements: 3.1_

- [ ] 1.2 (P) アダプター側でリモート取得の実装を追加する
  - リポジトリルートで `git fetch origin` コマンドを実行する
  - コマンド失敗時はエラーメッセージ付きでエラーを返す
  - 既存のコマンド実行ヘルパーを利用する
  - _Requirements: 1.1, 1.2, 3.2_

- [ ] 2. Issue 初期化時のワークツリー作成起点をリモート最新に変更する
  - worktree 作成の前にリモート取得を実行する
  - worktree の起点を設定ファイルの default branch に基づくリモートブランチ参照に変更する
  - リモート取得が失敗した場合は worktree 作成を行わずエラーを返す
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [ ] 3. テストの更新と追加
- [ ] 3.1 (P) 既存テストの mock に fetch メソッドの実装を追加する
  - インテグレーションテストで使用している mock の GitWorktree 実装に fetch メソッドを追加する
  - 既存テストがコンパイル・パスすることを確認する
  - _Requirements: 3.1_

- [ ] 3.2 initialize_issue の動作変更を検証するテストを追加する
  - fetch がワークツリー作成の前に呼ばれることを確認する
  - 起点がリモートの default branch を参照していることを確認する
  - fetch 失敗時にワークツリー作成が中断されることを確認する
  - _Requirements: 1.1, 1.2, 2.1, 2.2_
