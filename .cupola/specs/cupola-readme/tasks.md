# Implementation Plan

- [ ] 1. README の骨格と プロジェクト概要を作成する
- [ ] 1.1 README.md をリポジトリルートに作成し、タイトル、目次、プロジェクト概要セクションを記述する
  - プロジェクトタイトルとワンライナー説明を冒頭に配置する
  - 全セクションへの内部リンクを含む目次を作成する
  - Cupola が「GitHub Issue を起点に設計・実装を自動化するローカル常駐エージェント」であることを 3 文以内で記述する
  - 人間の役割（Issue 作成・ラベル付与・PR レビュー）と自動化範囲（設計ドキュメント生成〜実装〜レビュー対応）を区別して説明する
  - product.md の Core Capabilities と Value Proposition を情報源として使用する
  - _Requirements: 1.1, 1.2, 1.3_

- [ ] 2. 前提条件とインストール・セットアップ手順を記述する
- [ ] 2.1 前提条件セクションを記述する
  - 必須ツール（Rust stable, Claude Code CLI, gh CLI, Git, devbox）をテーブル形式で一覧にする
  - cc-sdd（spec-driven development）の概要を 1-2 文で説明し、Cupola が内部で cc-sdd を駆動する関係を記述する
  - devbox 利用時の一括セットアップ手順を案内する
  - _Requirements: 2.1, 2.2, 2.3_

- [ ] 2.2 インストール・セットアップ手順セクションを記述する
  - リポジトリクローンから `cupola run` 起動までの手順を番号付きリストで記述する
  - `cargo build` によるビルド手順を含める
  - `cupola.toml` の最小限の必須設定例を提供する（詳細は設定リファレンスセクションを参照）
  - `cupola init` による SQLite スキーマ初期化手順を含める
  - GitHub 上での `agent:ready` ラベル作成手順を含める
  - 手順に従えば `cupola run` が正常に polling を開始できる状態に至ること
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. 使い方セクションを記述する
- [ ] 3.1 Issue 作成から merge までのワークフロー全体をステップバイステップで記述する
  - 各ステップで人間の操作と Cupola の自動処理を明確に区別する（ラベル付きで記述）
  - `agent:ready` ラベル付与が Cupola のトリガーとなることを説明する
  - 設計 PR と実装 PR の 2 段階レビューフローを説明する
  - product.md のワークフロー情報を情報源として使用する
  - _Requirements: 4.1, 4.2, 4.3, 4.4_

- [ ] 4. CLI コマンドリファレンスと設定ファイルリファレンスを記述する
- [ ] 4.1 CLI コマンドリファレンスセクションを記述する
  - `run` サブコマンドの説明、全オプション（`--polling-interval-secs`, `--log-level`, `--config`）、実行例を記述する
  - `init` サブコマンドの説明と実行例を記述する
  - `status` サブコマンドの説明と実行例を記述する
  - 各コマンドの実行例をコードブロックで提供する
  - cli.rs の定義を正確に反映する
  - _Requirements: 5.1, 5.2, 5.3, 5.4_

- [ ] 4.2 設定ファイルリファレンスセクションを記述する
  - `cupola.toml` の全 9 設定項目（owner, repo, default_branch, language, polling_interval_secs, max_retries, stall_timeout_secs, [log] level, [log] dir）を網羅する
  - 各項目の型、デフォルト値、説明をテーブル形式で記述する
  - 完全な設定ファイル記述例をコードブロックで提供する
  - config.rs の Config 構造体を信頼できる情報源として使用する
  - _Requirements: 6.1, 6.2, 6.3_

- [ ] 5. アーキテクチャ概要とライセンス情報を記述する
- [ ] 5.1 アーキテクチャ概要セクションを記述する
  - Clean Architecture 4 レイヤー（domain / application / adapter / bootstrap）の構成を説明する
  - 各レイヤーの責務と依存方向（内向きのみ）を簡潔に説明する
  - `src/` 配下のディレクトリ構造を 2 階層までツリー形式で示す
  - structure.md の内容を情報源として使用する
  - _Requirements: 7.1, 7.2, 7.3_

- [ ] 5.2 ライセンスセクションを記述する
  - ライセンス情報をプレースホルダーとして記述する（LICENSE ファイル未作成のため）
  - LICENSE ファイル作成後にリンクを追加する旨を注記する
  - _Requirements: 8.1, 8.2_

- [ ] 6. README 全体の検証を行う
- [ ] 6.1 全要件の網羅性と内容の正確性を検証する
  - プロジェクト概要が 3 文以内であること
  - 全 CLI サブコマンド（run, init, status）が記述されていること
  - `cupola.toml` の全 9 設定項目が記述されていること
  - Clean Architecture 4 レイヤーが説明されていること
  - 目次の内部リンクが全セクションに対応していること
  - コードブロック内のコマンドが正確であること
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2, 4.3, 4.4, 5.1, 5.2, 5.3, 5.4, 6.1, 6.2, 6.3, 7.1, 7.2, 7.3, 8.1, 8.2_
