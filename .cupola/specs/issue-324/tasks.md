# Implementation Plan

- [ ] 1. 本番向けコードで `unwrap` を禁止する clippy 設定を整える
  - 既存の lint 方針を維持したまま、本番向けコードでは `unwrap` の使用が静的解析で検出される状態にする
  - 静的解析を実行し、ライブラリ対象で `unwrap` に起因する新たな違反がないことを確認する
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ] 2. テスト対象では `unwrap` を許容しつつ、品質チェックを通過できる状態にする
  - ライブラリクレート内の unit test では `unwrap` を利用できるようにし、本番向けコードの制約には影響しないようにする
  - `tests/` 配下の integration test についても、各 test クレートで `unwrap` を許容するか使用しない制約を適用し、全ターゲットを対象とした静的解析が通過する状態にする
  - テストを実行し、全テストがエラーなく通過することを確認する
  - 全ターゲットを対象に静的解析を実行し、追加のエラーが発生しないことを確認する
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
